# JDBC configuration for MySQL.
# Update these values to match your local MySQL instance.
db.url=jdbc:mysql://localhost:3306/smart_scheduler?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=root
db.password=UPES@krishna06
db.driver=com.mysql.cj.jdbc.Driver

# Default availability window for the smart scheduler (24h, HH:mm).
availability.start=16:00
availability.end=22:00

# Maximum continuous work block (minutes) before a break is inserted.
schedule.block.maxMinutes=50
schedule.break.minutes=10
